import {NavigationProp} from '@react-navigation/native';
import {Animated} from 'react-native';

export type NavigationType = NavigationProp<any, any>;

export type AnimatedValueType = Animated.Value;
