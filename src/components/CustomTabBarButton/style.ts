import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  text: {
    fontSize: 12,
    marginTop: 4,
  },
  focusedTextView: {
    alignItems: 'center',
  },
  unfocusedTextView: {
    borderBottomColor: 'transparent',
    alignItems: 'center',
  },
});
